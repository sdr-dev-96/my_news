{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="reset" class="btn btn-secondary btn-lg">Annuler</button>
    <button type="submit" class="btn btn-success btn-lg">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
